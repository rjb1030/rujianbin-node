#!/usr/bin/env node

/**
 * Module dependencies.
 * 如果windows启动node 先运行set命令，如： set PORT=3001 然后再运行node命令： node www
 * #!/usr/bin/env node 表示在linux下可以./启动
 */

require('./server3000')();
require('./server3001')();